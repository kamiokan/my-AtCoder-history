#include <iostream>
#include <climits>

using namespace std;

// int, long, long long の最大値を表示する
int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);

    cout << INT_MAX << endl;
    cout << LONG_MAX << endl;
    cout << LLONG_MAX << endl;

    return 0;
}
